// Code generated by fdb-go-layer-plugin. DO NOT EDIT.
// Source: github.com/romannikov/fdb-go-layer-plugin/tests/atomic

package atomic

import (
	"encoding/binary"
	"fmt"

	"github.com/apple/foundationdb/bindings/go/src/fdb"
	"github.com/apple/foundationdb/bindings/go/src/fdb/directory"
	"github.com/apple/foundationdb/bindings/go/src/fdb/tuple"
	"google.golang.org/protobuf/proto"
)

var _ = binary.LittleEndian

// CounterPaginationOptions represents options for paginated queries
type CounterPaginationOptions struct {
	Begin tuple.Tuple
	Limit int
}

// CounterPaginatedResult represents a paginated result set
type CounterPaginatedResult struct {
	Items   []*Counter
	NextKey tuple.Tuple
	HasMore bool
}

func (s *RecordStore) getCounterTypeID() (int64, error) {
	if s.metadata == nil {
		return 0, fmt.Errorf("metadata not initialized, call SyncMetadata first")
	}
	typeID, ok := s.metadata["Counter"]
	if !ok {
		return 0, fmt.Errorf("type Counter not found in metadata")
	}
	return typeID, nil
}

// CreateCounter creates a new Counter entity in the database.
func (s *RecordStore) CreateCounter(tr Transaction, dir directory.DirectorySubspace, entity *Counter) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}

	key := dir.Pack(tuple.Tuple{typeID, entity.Id})

	// Save atomic fields and zero them out for marshaling

	atomic_Value := entity.Value
	entity.Value = 0

	atomic_MaxValue := entity.MaxValue
	entity.MaxValue = 0

	atomic_MinValue := entity.MinValue
	entity.MinValue = 0

	value, err := proto.Marshal(entity)
	if err != nil {
		return err
	}

	// Restore atomic fields

	entity.Value = atomic_Value

	entity.MaxValue = atomic_MaxValue

	entity.MinValue = atomic_MinValue

	tr.Set(key, value)

	// Store atomic fields in separate keys

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 2})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_Value))
		tr.Set(fieldKey, buf)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 3})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_MaxValue))
		tr.Set(fieldKey, buf)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 4})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_MinValue))
		tr.Set(fieldKey, buf)
	}

	return nil
}

// GetCounter retrieves a Counter entity by its primary key.
func (s *RecordStore) GetCounter(tr fdb.ReadTransaction, dir directory.DirectorySubspace, Id string) (*Counter, error) {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return nil, err
	}

	key := dir.Pack(tuple.Tuple{typeID, Id})
	value := tr.Get(key).MustGet()
	if value == nil {
		return nil, fmt.Errorf("counter not found")
	}
	entity := &Counter{}
	err = proto.Unmarshal(value, entity)
	if err != nil {
		return nil, err
	}

	// Read atomic fields

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 2})
		fieldVal := tr.Get(fieldKey).MustGet()
		if fieldVal != nil {
			entity.Value = int64(binary.LittleEndian.Uint64(fieldVal))
		}
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 3})
		fieldVal := tr.Get(fieldKey).MustGet()
		if fieldVal != nil {
			entity.MaxValue = int64(binary.LittleEndian.Uint64(fieldVal))
		}
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 4})
		fieldVal := tr.Get(fieldKey).MustGet()
		if fieldVal != nil {
			entity.MinValue = int64(binary.LittleEndian.Uint64(fieldVal))
		}
	}

	return entity, nil
}

// SetCounter updates an existing Counter entity in the database.
func (s *RecordStore) SetCounter(tr Transaction, dir directory.DirectorySubspace, entity *Counter) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}

	key := dir.Pack(tuple.Tuple{typeID, entity.Id})

	// Clear stale index entries from the old version of the entity
	oldValue := tr.Get(key).MustGet()
	if oldValue != nil {
		old := &Counter{}
		if unmarshalErr := proto.Unmarshal(oldValue, old); unmarshalErr == nil {

		}
	}

	// Save atomic fields and zero them out for marshaling

	atomic_Value := entity.Value
	entity.Value = 0

	atomic_MaxValue := entity.MaxValue
	entity.MaxValue = 0

	atomic_MinValue := entity.MinValue
	entity.MinValue = 0

	value, err := proto.Marshal(entity)
	if err != nil {
		return err
	}

	// Restore atomic fields

	entity.Value = atomic_Value

	entity.MaxValue = atomic_MaxValue

	entity.MinValue = atomic_MinValue

	tr.Set(key, value)

	// Store atomic fields in separate keys

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 2})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_Value))
		tr.Set(fieldKey, buf)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 3})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_MaxValue))
		tr.Set(fieldKey, buf)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, entity.Id, "f", 4})
		buf := make([]byte, 8)
		binary.LittleEndian.PutUint64(buf, uint64(atomic_MinValue))
		tr.Set(fieldKey, buf)
	}

	return nil
}

// DeleteCounter removes a Counter entity from the database.
func (s *RecordStore) DeleteCounter(tr Transaction, dir directory.DirectorySubspace, Id string) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}

	key := dir.Pack(tuple.Tuple{typeID, Id})
	value := tr.Get(key).MustGet()
	if value != nil {
		entity := &Counter{}
		err := proto.Unmarshal(value, entity)
		if err == nil {

		}
	}
	tr.Clear(key)
	// Clear atomic fields

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 2})
		tr.Clear(fieldKey)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 3})
		tr.Clear(fieldKey)
	}

	{
		fieldKey := dir.Pack(tuple.Tuple{typeID, Id, "f", 4})
		tr.Clear(fieldKey)
	}

	return nil
}

// AddCounterValue applies an atomic mutation to the Value field of Counter.
func (s *RecordStore) AddCounterValue(tr Transaction, dir directory.DirectorySubspace, Id string, val int64) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}
	key := dir.Pack(tuple.Tuple{typeID, Id, "f", 2})

	buf := make([]byte, 8)
	binary.LittleEndian.PutUint64(buf, uint64(val))

	tr.AtomicOp(key, 2, buf)
	return nil
}

// MaxCounterMaxValue applies an atomic mutation to the MaxValue field of Counter.
func (s *RecordStore) MaxCounterMaxValue(tr Transaction, dir directory.DirectorySubspace, Id string, val int64) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}
	key := dir.Pack(tuple.Tuple{typeID, Id, "f", 3})

	buf := make([]byte, 8)
	binary.LittleEndian.PutUint64(buf, uint64(val))

	tr.AtomicOp(key, 12, buf)
	return nil
}

// MinCounterMinValue applies an atomic mutation to the MinValue field of Counter.
func (s *RecordStore) MinCounterMinValue(tr Transaction, dir directory.DirectorySubspace, Id string, val int64) error {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return err
	}
	key := dir.Pack(tuple.Tuple{typeID, Id, "f", 4})

	buf := make([]byte, 8)
	binary.LittleEndian.PutUint64(buf, uint64(val))

	tr.AtomicOp(key, 13, buf)
	return nil
}

// BatchGetCounter retrieves multiple Counter entities by their primary keys.
func (s *RecordStore) BatchGetCounter(tr fdb.ReadTransaction, dir directory.DirectorySubspace, ids []tuple.Tuple) (map[string]*Counter, error) {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return nil, err
	}

	result := make(map[string]*Counter)
	futures := make([]fdb.FutureByteSlice, len(ids))

	for i, id := range ids {
		keyTpl := append(tuple.Tuple{typeID}, id...)
		key := dir.Pack(keyTpl)
		futures[i] = tr.Get(key)
	}

	for i, future := range futures {
		value := future.MustGet()
		if value == nil {
			continue
		}
		entity := &Counter{}
		err := proto.Unmarshal(value, entity)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal entity at index %d: %w", i, err)
		}
		result[ids[i].String()] = entity
	}

	return result, nil
}

// ListCounter retrieves a list of Counter entities starting from the given key.
func (s *RecordStore) ListCounter(tr fdb.ReadTransaction, dir directory.DirectorySubspace, opts CounterPaginationOptions) (*CounterPaginatedResult, error) {
	typeID, err := s.getCounterTypeID()
	if err != nil {
		return nil, err
	}

	result := &CounterPaginatedResult{
		Items: make([]*Counter, 0),
	}

	beginTpl := append(tuple.Tuple{typeID}, opts.Begin...)
	begin := dir.Pack(beginTpl)

	// Scan all keys under typeID. We request extra rows to account for
	// index entries that will be filtered out.
	typePrefix := dir.Pack(tuple.Tuple{typeID})
	typePrefixRange, err := fdb.PrefixRange(typePrefix)
	if err != nil {
		return nil, err
	}

	iter := tr.GetRange(fdb.KeyRange{
		Begin: begin,
		End:   typePrefixRange.End,
	}, fdb.RangeOptions{
		Reverse: false,
	}).Iterator()

	var nextKey fdb.Key
	for iter.Advance() {
		kv := iter.MustGet()

		// Skip index entries: their second tuple element is the string "index".
		tpl, err := dir.Unpack(kv.Key)
		if err != nil {
			return nil, fmt.Errorf("failed to unpack key: %w", err)
		}
		if len(tpl) >= 2 {
			if s, ok := tpl[1].(string); ok && s == "index" {
				continue
			}
		}

		entity := &Counter{}
		err = proto.Unmarshal(kv.Value, entity)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal entity: %w", err)
		}
		result.Items = append(result.Items, entity)
		nextKey = kv.Key

		// Stop once we have enough items for pagination check
		if len(result.Items) > opts.Limit {
			break
		}
	}

	result.HasMore = len(result.Items) > opts.Limit
	if result.HasMore {
		tpl, err := dir.Unpack(nextKey)
		if err != nil {
			return nil, fmt.Errorf("failed to unpack next key: %w", err)
		}
		// Remove typeID to return just the PK tuple
		result.NextKey = tpl[1:]
		result.Items = result.Items[:opts.Limit]
	}

	return result, nil
}

type CounterRepository struct {
	store *RecordStore
}

func NewCounterRepository(store *RecordStore) *CounterRepository {
	return &CounterRepository{store: store}
}

func (r *CounterRepository) Create(tr Transaction, dir directory.DirectorySubspace, entity *Counter) error {
	return r.store.CreateCounter(tr, dir, entity)
}

func (r *CounterRepository) Get(tr fdb.ReadTransaction, dir directory.DirectorySubspace, key tuple.Tuple) (*Counter, error) {
	return r.store.GetCounter(tr, dir, key[0].(string))
}

func (r *CounterRepository) Set(tr Transaction, dir directory.DirectorySubspace, entity *Counter) error {
	return r.store.SetCounter(tr, dir, entity)
}

func (r *CounterRepository) Delete(tr Transaction, dir directory.DirectorySubspace, key tuple.Tuple) error {
	return r.store.DeleteCounter(tr, dir, key[0].(string))
}

func (r *CounterRepository) BatchGet(tr fdb.ReadTransaction, dir directory.DirectorySubspace, keys []tuple.Tuple) (map[string]*Counter, error) {
	return r.store.BatchGetCounter(tr, dir, keys)
}

func (r *CounterRepository) List(tr fdb.ReadTransaction, dir directory.DirectorySubspace, opts PaginationOptions) (*PaginatedResult[Counter], error) {
	specificOpts := CounterPaginationOptions{Begin: opts.Begin, Limit: opts.Limit}
	res, err := r.store.ListCounter(tr, dir, specificOpts)
	if err != nil {
		return nil, err
	}
	return &PaginatedResult[Counter]{Items: res.Items, NextKey: res.NextKey, HasMore: res.HasMore}, nil
}
